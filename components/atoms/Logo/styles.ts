import styled from "styled-components";

export const LogoContainer = styled.article`
  font-family: ${({ theme }) => theme.font.family.kosugi};
  font-size: 24px;
  text-transform: uppercase;
  user-select: none;
`;